services:
  - type: web
    name: collaborative-whiteboard
    runtime: node
    buildCommand: NODE_ENV=development pnpm install --frozen-lockfile && pnpm --filter @whiteboard/web build
    startCommand: NODE_ENV=production npx tsx server/src/index.ts
    envVars:
      - key: PORT
        value: 8080
    plan: free
